/**
 * App.tsx
 *
 * Entry point for running this module as a standalone app on a
 * device/emulator. Just re-exports the illustrative example wiring -
 * a real host app would render StudyRoomsScreen itself instead.
 */
import ExampleUsage from './example/ExampleUsage';

export default ExampleUsage;
